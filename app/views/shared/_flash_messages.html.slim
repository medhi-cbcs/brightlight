/ - flash.each do |key, value|
/   div class="alert alert-dismissible alert-#{ key}"
/     button.close type="button" data-dismiss="alert" &times;
/     = value
- flash.each do |key, value|
	javascript:
		Materialize.toast('#{value}', 4000, '#{key=='error'? "red" : "light-green"}');